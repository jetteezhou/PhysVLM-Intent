# 项目结构说明

## 📁 目录结构

```
PhysVLM-Intent/
├── pipeline/                    # Pipeline核心模块
│   ├── __init__.py
│   ├── pipeline.py             # 主Pipeline类（IntentLabelPipeline）
│   ├── audio_processor.py      # 音频处理模块（ASR）
│   ├── video_processor.py      # 视频处理模块（分割、帧采样）
│   └── llm_client.py          # LLM客户端模块（意图分析、目标定位）
│
├── config/                      # 配置模块
│   ├── __init__.py
│   └── settings.py             # 配置和常量
│
├── utils/                       # 工具函数模块
│   ├── __init__.py
│   └── image_utils.py          # 图像处理工具
│
├── tools/                       # 工具和脚本
│   ├── annotation/             # 标注工具
│   │   ├── annotation_server.py      # Flask服务器
│   │   ├── annotation_tool.html      # 前端页面
│   │   ├── start_annotation_tool.py  # 启动脚本
│   │   └── requirements_annotation.txt
│   └── sam/                    # SAM分割工具
│       └── sam_test.py         # SAM测试脚本
│
├── docs/                        # 文档
│   ├── README_PIPELINE.md      # Pipeline使用说明
│   ├── README_annotation.md    # 标注工具使用说明
│   └── QUICK_START.md          # 快速开始指南
│
├── models/                      # 模型文件
│   └── sam2.1_b.pt            # SAM模型文件
│
├── data_label_gen_pipeline.py  # 主入口文件（向后兼容）
├── start_annotation.py         # 启动标注工具（便捷脚本）
├── requirements.txt            # 项目依赖
└── README.md                   # 项目主文档
```

## 📂 目录说明

### pipeline/
Pipeline核心功能模块，包含完整的意图推理和目标定位流程。

### config/
配置管理模块，统一管理API密钥、模型配置等。

### utils/
通用工具函数模块。

### tools/
各种工具和脚本：
- **annotation/**: Web标注工具，用于人工检查和修正AI生成的结果
- **sam/**: SAM（Segment Anything Model）视频分割工具

### docs/
项目文档目录。

### models/
模型文件存储目录。

## 🚀 使用方式

### 运行Pipeline

```bash
python data_label_gen_pipeline.py
```

### 启动标注工具

```bash
# 方式1：使用便捷脚本
python start_annotation.py

# 方式2：直接运行
python tools/annotation/start_annotation_tool.py
```

### 运行SAM测试

```bash
python tools/sam/sam_test.py
```

## 📝 注意事项

1. **路径引用**：所有工具脚本都已更新为使用项目根目录的相对路径，确保在任何位置运行都能正常工作。

2. **数据文件**：Pipeline生成的数据文件（`pipeline_data.json`）位于项目根目录。

3. **输出目录**：
   - `output_frames/`: Pipeline生成的视频帧图像
   - `annotation_backups/`: 标注工具的备份文件
   - `runs/`: SAM工具的输出结果

4. **测试数据**：测试数据位于 `test_data/` 目录。

